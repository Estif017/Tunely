{
  "name": "tunely-stream-backend",
  "version": "1.0.0",
  "description": "Tiny stream-resolver backend for Tunely — resolves YouTube video IDs to direct audio URLs using yt-dlp.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.14.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
